{
  "name": "storytrackr",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node scripts/dev.mjs",
    "check": "node --check api/[...route].js",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.2"
  }
}
